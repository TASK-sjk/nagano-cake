<table class="table table-hover table-inverse" style="margin-top:40px; ">

	<tr>
		<th>ジャンル検索</th>
	</tr>
	<% @categories.each do |category| %>
	<tr>
		<th><%= link_to category.name, items_path(category_id: category.id) %>
		</th>
	</tr>
	<% end %>
</table>
